package TestCollection2;

import java.util.HashSet;
import java.util.Set;

/**
 * 测试hashSet的基本用法(底层其实作为HashMap的key,所以值是不能重复的)
 * 无序，且元素不可重复，null最多也只有一个，其余和list一样
 * List有顺序并且可重复
 */
public class TestHashSet {
      Set<String> set=new HashSet<>();

}
